-- Generado por wallust — colors de bordes hyprland.
-- theme.lua hace dofile() de este archivo para aplicar bordes.

-- Alpha 80 (50%) en active y 60 (38%) en inactive para mantener el look soft del original.
hl.config({
  general = {
    col = {
      active_border   = { colors = { "rgba({{color5[1:]}}80)", "rgba({{color4[1:]}}80)" }, angle = 45 },
      inactive_border = { colors = { "rgba({{color8[1:]}}60)", "rgba({{color8[1:]}}60)" }, angle = 45 },
    },
  },
})
