[mgr]
ratio          = [1, 3, 4]
sort_by        = "mtime"
sort_sensitive = false
sort_reverse   = true
sort_dir_first = true
sort_translit  = false
linemode       = "size"
show_hidden    = false
show_symlink   = true
scrolloff      = 5
mouse_events   = ["click", "scroll"]
title_format   = "Yazi: {cwd}"

[preview]
wrap            = "no"
tab_size        = 2
max_width       = 1000
max_height      = 1200
cache_dir       = ""
image_delay     = 30
image_filter    = "triangle"
image_quality   = 80
ueberzug_scale  = 1
ueberzug_offset = [0, 0, 0, 0]

[opener]
# Default editor — siempre nvim (bloquea yazi hasta salir)
edit = [
  { run = 'nvim "$@"', desc = "Edit in nvim", block = true, for = "unix" },
]
# nvim en una ventana nueva de kitty (no bloquea yazi)
edit_window = [
  { run = 'kitty -e nvim "$@"', desc = "nvim (new kitty)", orphan = true, for = "unix" },
]
# xdg-open queda como fallback opcional ("o" -> elegir opener)
open = [
  { run = 'xdg-open "$@"', desc = "Open with xdg", orphan = true, for = "linux" },
]
reveal = [
  { run = 'nautilus "$1"', desc = "Reveal in Nautilus", orphan = true, for = "linux" },
]
extract = [
  { run = 'ouch d -y "$@"', desc = "Extract here (ouch)", for = "unix" },
]
play = [
  { run = 'mpv --force-window "$@"', orphan = true, desc = "Play with mpv", for = "unix" },
]

# Enter abre TODO con nvim por default.
# Para imagen/video/audio dejo "play"/"open" como segundas opciones accesibles
# vía `o` (yazi muestra menú cuando hay más de un opener).
[open]
rules = [
  { mime = "inode/directory",             use = [ "edit" ] },
  { mime = "text/*",                      use = [ "edit", "reveal" ] },
  { mime = "application/json",            use = [ "edit", "reveal" ] },
  { mime = "application/xml",             use = [ "edit", "reveal" ] },
  { mime = "application/javascript",      use = [ "edit", "reveal" ] },
  { mime = "application/x-shellscript",   use = [ "edit", "reveal" ] },
  { mime = "image/*",                     use = [ "edit", "open", "reveal" ] },
  { mime = "video/*",                     use = [ "edit", "play", "open" ] },
  { mime = "audio/*",                     use = [ "edit", "play", "open" ] },
  { mime = "application/x-bittorrent",    use = [ "play" ] },
  { mime = "application/zip",             use = [ "extract", "reveal" ] },
  { mime = "application/gzip",            use = [ "extract", "reveal" ] },
  { mime = "application/x-tar",           use = [ "extract", "reveal" ] },
  { mime = "application/x-7z-compressed", use = [ "extract", "reveal" ] },
  { mime = "application/x-rar",           use = [ "extract", "reveal" ] },
  { mime = "application/xz",              use = [ "extract", "reveal" ] },
  { mime = "application/x-zstd",          use = [ "extract", "reveal" ] },
  { mime = "*",                           use = [ "edit", "open", "reveal" ] },
]

[tasks]
micro_workers    = 10
macro_workers    = 25
bizarre_retry    = 5
image_alloc      = 536870912 # 512MB
image_bound      = [0, 0]
suppress_preload = false

[input]
cursor_blink = true

[which]
sort_by      = "key"
sort_sensitive = false
sort_reverse   = false
